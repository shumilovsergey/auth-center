services:
  auth:
    build:
      context: .
      target: dev
    ports:
      - "8886:8886"
    env_file: .env
    volumes:
      - ./build:/app
      - go-cache:/root/go/pkg/mod

volumes:
  go-cache:
