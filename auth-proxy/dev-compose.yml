services:
  proxy:
    build:
      context: .
      target: dev
    ports:
      - "8080:8080"
    env_file: .env
    volumes:
      - ./build:/app
      - go-cache:/root/go/pkg/mod

volumes:
  go-cache:
