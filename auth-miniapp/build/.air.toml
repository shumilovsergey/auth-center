root = "."
tmp_dir = "tmp"

[build]
  cmd = "mkdir -p /tmp/airbuild && go build -o /tmp/airbuild/auth-miniapp ."
  bin = "/tmp/airbuild/auth-miniapp"
  include_ext = ["go", "js", "css", "html", "svg"]
  exclude_dir = ["bin", "tmp"]
  delay = 200

[misc]
  clean_on_exit = true
