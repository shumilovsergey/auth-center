services:
  miniapp:
    build:
      context: .
      target: dev
    ports:
      - "8892:8892"
    env_file: .env
    volumes:
      - ./build:/app
      - go-cache:/root/go/pkg/mod

volumes:
  go-cache:

# docker compose -f dev-compose.yml up --build
